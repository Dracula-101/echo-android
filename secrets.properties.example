# ──────────────────────────────────────────────────────────────────────────────
# Echo Android — secrets.properties
# ──────────────────────────────────────────────────────────────────────────────
# Copy this file to `secrets.properties` in the project root and fill in
# the values. This file is required for the build to succeed.
#
#   cp secrets.properties.example secrets.properties
#
# ──────────────────────────────────────────────────────────────────────────────

# Backend URL for the DEVELOPMENT flavor.
# Used when building with the "development" product flavor.
# Falls back to http://localhost:8080 if not set.
DEV_BACKEND_URL=http://localhost:8080

# Backend URL for the PRODUCTION flavor.
# Used when building with the "production" product flavor.
# This is REQUIRED — no default fallback.
PROD_BACKEND_URL=https://your-production-backend.example.com
